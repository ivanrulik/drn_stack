services:
  ros-viz:
    environment:
      DRN_GPU_ACCELERATION: software

  px4-sitl:
    environment:
      DRN_GPU_ACCELERATION: software
    volumes:
      - type: bind
        source: ./profiles/x500-precision-land/models/mono_cam/model.sdf
        target: /opt/PX4-Autopilot/Tools/simulation/gz/models/mono_cam/model.sdf
        read_only: true
